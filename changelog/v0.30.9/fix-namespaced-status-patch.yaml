changelog:
  - type: FIX
    issueLink: https://github.com/solo-io/solo-kit/issues/529
    resolvesIssue: false
    description: >
      Fix error that would occur if controllers in multiple namespaces were trying to update the status on a shared resource.
      `GetJsonPatchData` now checks the POD_NAMESPACE environment variable to determine which namespace to modify.
